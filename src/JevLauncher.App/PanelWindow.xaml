<Window x:Class="JevLauncher.App.PanelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="False" ShowInTaskbar="False"
        Topmost="True" ResizeMode="NoResize" SizeToContent="Height"
        Width="640" Background="#01000000" WindowStartupLocation="Manual"
        PreviewKeyDown="OnPreviewKeyDown"
        Deactivated="OnDeactivated"
        Activated="OnActivated"
        Loaded="OnLoaded">
  <Border CornerRadius="12" Background="#E6111118" Padding="0">
    <StackPanel>
      <TextBox x:Name="QueryBox" FontSize="22" BorderThickness="0"
               Background="Transparent" Padding="16,12" Foreground="#F0F0F5"
               CaretBrush="#F0F0F5" TextChanged="OnQueryChanged"/>
      <ItemsControl x:Name="Rows" MaxHeight="308">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border x:Name="RowBorder" Padding="12,8" Margin="6,1" CornerRadius="8" Background="Transparent">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="34"/>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Glyph}" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                           FontSize="18" Foreground="#C8C8D4" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                  <TextBlock Text="{Binding Title}" Foreground="#F0F0F5" TextTrimming="CharacterEllipsis"/>
                  <TextBlock Text="{Binding Detail}" Foreground="#8A8A99" FontSize="11"
                             TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="&#xE73E;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                           Foreground="#3DDC84" FontSize="16" VerticalAlignment="Center"
                           Visibility="{Binding ReadyVisibility}"/>
              </Grid>
            </Border>
            <DataTemplate.Triggers>
              <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter TargetName="RowBorder" Property="Background" Value="#22FFFFFF"/>
              </DataTrigger>
            </DataTemplate.Triggers>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
      <Grid x:Name="Footer" Height="36" Margin="16,0">
        <TextBlock x:Name="FooterLeft" Foreground="#8A8A99" VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis"/>
        <TextBlock x:Name="FooterRight" Foreground="#8A8A99" HorizontalAlignment="Right"
                   VerticalAlignment="Center"/>
      </Grid>
    </StackPanel>
  </Border>
</Window>
